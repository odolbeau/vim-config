" Indentation
    setlocal softtabstop=2  " tab = 4 space
    setlocal shiftwidth=2
    setlocal expandtab      " no more tabs, only spaces!

" Editor
    setlocal list
    setlocal listchars=trail:¤,tab:>-,nbsp:•     " Show blank spaces and tabs at the end of a line
